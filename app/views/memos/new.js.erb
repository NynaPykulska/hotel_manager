$("#modal-add-memo").html("<%= escape_javascript(render 'new') %>")
$("#modal-add-memo").modal("show")

$("#recurring_check_box").change(function() {
    if(this.checked)
    {
        $("#new_memo_date_field").hide()
        $("#new_memo_recurring_options").show()
        $("#new_memo_recurring_date_pickers").show()
    }
    else
    {
        $("#new_memo_date_field").show()
        $("#new_memo_recurring_options").hide()
        $("#new_memo_recurring_date_pickers").hide()         
    }
});

$("#memo_recurrence").change(function() {
    if($(this).val() == 4){
        $("#new_memo_recurring_additional").show()
    } else {
        $("#new_memo_recurring_additional").hide()
    }
});

$('#new_memo_date_field .input-group.date').datepicker({
    format: 'yyyy-mm-dd',
    language: "pl",
    autoclose: true,
    todayHighlight: true,
    todayBtn: "linked"
}).on('changeDate', function (a) {
    
});

$('#new_memo_start_date_field .input-group.date').datepicker({
    format: 'yyyy-mm-dd',
    language: "pl",
    autoclose: true,
    todayHighlight: true,
    todayBtn: "linked"
}).on('changeDate', function (a) {
    
});

$('#new_memo_end_date_field .input-group.date').datepicker({
    format: 'yyyy-mm-dd',
    language: "pl",
    autoclose: true,
    todayHighlight: true,
    todayBtn: "linked"
}).on('changeDate', function (a) {
    
});
